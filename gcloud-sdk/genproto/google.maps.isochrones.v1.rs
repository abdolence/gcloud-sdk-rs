// This file is @generated by prost-build.
/// A request to generate a single isochrone.
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct GenerateIsochroneRequest {
    /// Required. The travel time for the isochrone calculation. The value must
    /// be positive and is capped at 7200 seconds (120 minutes).
    /// For DRIVE mode, the maximum allowed duration is 3600 seconds (60 minutes).
    #[prost(message, optional, tag = "3")]
    pub travel_duration: ::core::option::Option<::prost_types::Duration>,
    /// Required. The mode of transportation.
    #[prost(enumeration = "generate_isochrone_request::TravelMode", tag = "4")]
    pub travel_mode: i32,
    /// Required. The direction of travel.
    #[prost(enumeration = "generate_isochrone_request::TravelDirection", tag = "5")]
    pub travel_direction: i32,
    /// Optional. Specifies the preference for how to route. Defaults to
    /// TRAFFIC_UNAWARE.
    #[prost(enumeration = "generate_isochrone_request::RoutingPreference", tag = "6")]
    pub routing_preference: i32,
    /// Optional. Specifies whether to smooth the edges of the resulting isochrone
    /// polygons.
    #[prost(bool, tag = "7")]
    pub enable_smoothing: bool,
    /// Optional. Controls the precision of the generated polygon.
    /// Defaults to POLYGON_FIDELITY_UNSPECIFIED.
    #[prost(enumeration = "generate_isochrone_request::PolygonFidelity", tag = "8")]
    pub polygon_fidelity: i32,
    /// Required. The starting point for the isochrone calculation.
    #[prost(oneof = "generate_isochrone_request::Origin", tags = "1, 2")]
    pub origin: ::core::option::Option<generate_isochrone_request::Origin>,
}
/// Nested message and enum types in `GenerateIsochroneRequest`.
pub mod generate_isochrone_request {
    /// Defines the mode of transportation for isochrone calculation.
    #[derive(
        Clone,
        Copy,
        Debug,
        PartialEq,
        Eq,
        Hash,
        PartialOrd,
        Ord,
        ::prost::Enumeration
    )]
    #[repr(i32)]
    pub enum TravelMode {
        /// No travel mode specified.
        Unspecified = 0,
        /// Travel by passenger car.
        Drive = 1,
        /// Travel by bicycle.
        Bicycle = 2,
        /// Travel by walking.
        Walk = 3,
    }
    impl TravelMode {
        /// String value of the enum field names used in the ProtoBuf definition.
        ///
        /// The values are not transformed in any way and thus are considered stable
        /// (if the ProtoBuf definition does not change) and safe for programmatic use.
        pub fn as_str_name(&self) -> &'static str {
            match self {
                Self::Unspecified => "TRAVEL_MODE_UNSPECIFIED",
                Self::Drive => "DRIVE",
                Self::Bicycle => "BICYCLE",
                Self::Walk => "WALK",
            }
        }
        /// Creates an enum from field names used in the ProtoBuf definition.
        pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
            match value {
                "TRAVEL_MODE_UNSPECIFIED" => Some(Self::Unspecified),
                "DRIVE" => Some(Self::Drive),
                "BICYCLE" => Some(Self::Bicycle),
                "WALK" => Some(Self::Walk),
                _ => None,
            }
        }
    }
    /// Specifies the direction of travel for the isochrone calculation.
    #[derive(
        Clone,
        Copy,
        Debug,
        PartialEq,
        Eq,
        Hash,
        PartialOrd,
        Ord,
        ::prost::Enumeration
    )]
    #[repr(i32)]
    pub enum TravelDirection {
        /// No travel direction specified.
        Unspecified = 0,
        /// Calculates the area reachable *from* the origin point.
        /// Example: "Where can I deliver to from my warehouse in 30 minutes?"
        From = 1,
        /// Calculates the area from which you can travel *to* the origin point.
        /// Example: "Where can my employees commute from to reach the office in 30
        /// minutes?"
        To = 2,
    }
    impl TravelDirection {
        /// String value of the enum field names used in the ProtoBuf definition.
        ///
        /// The values are not transformed in any way and thus are considered stable
        /// (if the ProtoBuf definition does not change) and safe for programmatic use.
        pub fn as_str_name(&self) -> &'static str {
            match self {
                Self::Unspecified => "TRAVEL_DIRECTION_UNSPECIFIED",
                Self::From => "FROM",
                Self::To => "TO",
            }
        }
        /// Creates an enum from field names used in the ProtoBuf definition.
        pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
            match value {
                "TRAVEL_DIRECTION_UNSPECIFIED" => Some(Self::Unspecified),
                "FROM" => Some(Self::From),
                "TO" => Some(Self::To),
                _ => None,
            }
        }
    }
    /// Determines how traffic conditions are incorporated into the calculation.
    #[derive(
        Clone,
        Copy,
        Debug,
        PartialEq,
        Eq,
        Hash,
        PartialOrd,
        Ord,
        ::prost::Enumeration
    )]
    #[repr(i32)]
    pub enum RoutingPreference {
        /// No routing preference specified. The server will use its default,
        /// which is TRAFFIC_UNAWARE.
        Unspecified = 0,
        /// The calculation will not take traffic conditions into consideration.
        /// The isochrone will be based on the road network and static travel times.
        /// This is suitable for planning purposes where traffic is not a factor.
        TrafficUnaware = 1,
        /// The calculation will factor in live traffic conditions.
        TrafficAware = 2,
    }
    impl RoutingPreference {
        /// String value of the enum field names used in the ProtoBuf definition.
        ///
        /// The values are not transformed in any way and thus are considered stable
        /// (if the ProtoBuf definition does not change) and safe for programmatic use.
        pub fn as_str_name(&self) -> &'static str {
            match self {
                Self::Unspecified => "ROUTING_PREFERENCE_UNSPECIFIED",
                Self::TrafficUnaware => "TRAFFIC_UNAWARE",
                Self::TrafficAware => "TRAFFIC_AWARE",
            }
        }
        /// Creates an enum from field names used in the ProtoBuf definition.
        pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
            match value {
                "ROUTING_PREFERENCE_UNSPECIFIED" => Some(Self::Unspecified),
                "TRAFFIC_UNAWARE" => Some(Self::TrafficUnaware),
                "TRAFFIC_AWARE" => Some(Self::TrafficAware),
                _ => None,
            }
        }
    }
    /// Controls the level of detail in the isochrone polygon.
    #[derive(
        Clone,
        Copy,
        Debug,
        PartialEq,
        Eq,
        Hash,
        PartialOrd,
        Ord,
        ::prost::Enumeration
    )]
    #[repr(i32)]
    pub enum PolygonFidelity {
        /// No polygon fidelity specified. The server will use its default, which
        /// is based on the travel duration.
        Unspecified = 0,
        /// Low precision. Good for covering large areas with fewer vertices.
        Low = 1,
        /// Medium precision. A balance between detail and artifact size.
        Medium = 2,
        /// High precision. High fidelity edges, but may produce holes in the polygon
        /// where the road network density is low.
        High = 3,
    }
    impl PolygonFidelity {
        /// String value of the enum field names used in the ProtoBuf definition.
        ///
        /// The values are not transformed in any way and thus are considered stable
        /// (if the ProtoBuf definition does not change) and safe for programmatic use.
        pub fn as_str_name(&self) -> &'static str {
            match self {
                Self::Unspecified => "POLYGON_FIDELITY_UNSPECIFIED",
                Self::Low => "LOW",
                Self::Medium => "MEDIUM",
                Self::High => "HIGH",
            }
        }
        /// Creates an enum from field names used in the ProtoBuf definition.
        pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
            match value {
                "POLYGON_FIDELITY_UNSPECIFIED" => Some(Self::Unspecified),
                "LOW" => Some(Self::Low),
                "MEDIUM" => Some(Self::Medium),
                "HIGH" => Some(Self::High),
                _ => None,
            }
        }
    }
    /// Required. The starting point for the isochrone calculation.
    #[derive(Clone, PartialEq, ::prost::Oneof)]
    pub enum Origin {
        /// The origin as a latitude/longitude coordinate.
        #[prost(message, tag = "1")]
        Location(super::super::super::super::r#type::LatLng),
        /// The resource name of a place, in the `places/{place_id}` format.
        #[prost(string, tag = "2")]
        Place(::prost::alloc::string::String),
    }
}
/// A response containing the generated isochrone data.
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct GenerateIsochroneResponse {
    /// Output only. The generated isochrone.
    #[prost(message, optional, tag = "1")]
    pub isochrone: ::core::option::Option<Isochrone>,
}
/// The result of an isochrone calculation, representing an area of reachability
/// from an origin point within a specified travel time.
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct Isochrone {
    /// The geometric representation of the isochrone.
    #[prost(oneof = "isochrone::Geometry", tags = "1")]
    pub geometry: ::core::option::Option<isochrone::Geometry>,
}
/// Nested message and enum types in `Isochrone`.
pub mod isochrone {
    /// The geometric representation of the isochrone.
    #[derive(Clone, PartialEq, ::prost::Oneof)]
    pub enum Geometry {
        /// Output only. The isochrone geometry in GeoJSON format, using the RFC 7946
        /// format: <https://datatracker.ietf.org/doc/html/rfc7946#section-3.1.6.>
        #[prost(message, tag = "1")]
        GeoJson(::prost_types::Struct),
    }
}
/// Generated client implementations.
pub mod isochrone_service_client {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    use tonic::codegen::http::Uri;
    /// Service for calculating isochrones. An isochrone is an area of reachability
    /// from a given origin point within a specified travel time.
    #[derive(Debug, Clone)]
    pub struct IsochroneServiceClient<T> {
        inner: tonic::client::Grpc<T>,
    }
    impl IsochroneServiceClient<tonic::transport::Channel> {
        /// Attempt to create a new client by connecting to a given endpoint.
        pub async fn connect<D>(dst: D) -> Result<Self, tonic::transport::Error>
        where
            D: TryInto<tonic::transport::Endpoint>,
            D::Error: Into<StdError>,
        {
            let conn = tonic::transport::Endpoint::new(dst)?.connect().await?;
            Ok(Self::new(conn))
        }
    }
    impl<T> IsochroneServiceClient<T>
    where
        T: tonic::client::GrpcService<tonic::body::Body>,
        T::Error: Into<StdError>,
        T::ResponseBody: Body<Data = Bytes> + std::marker::Send + 'static,
        <T::ResponseBody as Body>::Error: Into<StdError> + std::marker::Send,
    {
        pub fn new(inner: T) -> Self {
            let inner = tonic::client::Grpc::new(inner);
            Self { inner }
        }
        pub fn with_origin(inner: T, origin: Uri) -> Self {
            let inner = tonic::client::Grpc::with_origin(inner, origin);
            Self { inner }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> IsochroneServiceClient<InterceptedService<T, F>>
        where
            F: tonic::service::Interceptor,
            T::ResponseBody: Default,
            T: tonic::codegen::Service<
                http::Request<tonic::body::Body>,
                Response = http::Response<
                    <T as tonic::client::GrpcService<tonic::body::Body>>::ResponseBody,
                >,
            >,
            <T as tonic::codegen::Service<
                http::Request<tonic::body::Body>,
            >>::Error: Into<StdError> + std::marker::Send + std::marker::Sync,
        {
            IsochroneServiceClient::new(InterceptedService::new(inner, interceptor))
        }
        /// Compress requests with the given encoding.
        ///
        /// This requires the server to support it otherwise it might respond with an
        /// error.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.send_compressed(encoding);
            self
        }
        /// Enable decompressing responses.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.inner = self.inner.accept_compressed(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_decoding_message_size(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.inner = self.inner.max_encoding_message_size(limit);
            self
        }
        /// Calculates and returns a single isochrone for a given set of parameters.
        pub async fn generate_isochrone(
            &mut self,
            request: impl tonic::IntoRequest<super::GenerateIsochroneRequest>,
        ) -> std::result::Result<
            tonic::Response<super::GenerateIsochroneResponse>,
            tonic::Status,
        > {
            self.inner
                .ready()
                .await
                .map_err(|e| {
                    tonic::Status::unknown(
                        format!("Service was not ready: {}", e.into()),
                    )
                })?;
            let codec = tonic_prost::ProstCodec::default();
            let path = http::uri::PathAndQuery::from_static(
                "/google.maps.isochrones.v1.IsochroneService/GenerateIsochrone",
            );
            let mut req = request.into_request();
            req.extensions_mut()
                .insert(
                    GrpcMethod::new(
                        "google.maps.isochrones.v1.IsochroneService",
                        "GenerateIsochrone",
                    ),
                );
            self.inner.unary(req, path, codec).await
        }
    }
}
