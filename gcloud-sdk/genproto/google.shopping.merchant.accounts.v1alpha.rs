// This file is @generated by prost-build.
/// Settings related to the verification email that is sent after adding a user.
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct VerificationMailSettings {
    /// Optional. Mode of the verification mail. If not set, the default is
    /// `SEND_VERIFICATION_MAIL`.
    #[prost(enumeration = "verification_mail_settings::VerificationMailMode", tag = "1")]
    pub verification_mail_mode: i32,
}
/// Nested message and enum types in `VerificationMailSettings`.
pub mod verification_mail_settings {
    /// The different configuration options for sending a verification email
    /// when adding a user.
    #[derive(
        Clone,
        Copy,
        Debug,
        PartialEq,
        Eq,
        Hash,
        PartialOrd,
        Ord,
        ::prost::Enumeration
    )]
    #[repr(i32)]
    pub enum VerificationMailMode {
        /// Default first member of every enum. Do not use.
        Unspecified = 0,
        /// An invitation email is sent to the user added shortly after.
        SendVerificationMail = 1,
        /// No invitation email is sent. This can be useful if the user is
        /// expected to accept the invitation through the API without needing
        /// another notification.
        SuppressVerificationMail = 2,
    }
    impl VerificationMailMode {
        /// String value of the enum field names used in the ProtoBuf definition.
        ///
        /// The values are not transformed in any way and thus are considered stable
        /// (if the ProtoBuf definition does not change) and safe for programmatic use.
        pub fn as_str_name(&self) -> &'static str {
            match self {
                Self::Unspecified => "VERIFICATION_MAIL_MODE_UNSPECIFIED",
                Self::SendVerificationMail => "SEND_VERIFICATION_MAIL",
                Self::SuppressVerificationMail => "SUPPRESS_VERIFICATION_MAIL",
            }
        }
        /// Creates an enum from field names used in the ProtoBuf definition.
        pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
            match value {
                "VERIFICATION_MAIL_MODE_UNSPECIFIED" => Some(Self::Unspecified),
                "SEND_VERIFICATION_MAIL" => Some(Self::SendVerificationMail),
                "SUPPRESS_VERIFICATION_MAIL" => Some(Self::SuppressVerificationMail),
                _ => None,
            }
        }
    }
}
